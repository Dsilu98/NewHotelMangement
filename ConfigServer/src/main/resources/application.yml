spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Dsilu98/MicroServiceConfig.git
          clone-on-start: true
        
server:
  port: 8081

